#pragma once

#include <iostream>
#include <fstream>
#include <sstream>

namespace engine
{
	using std::cerr;
	using std::cout;
	using std::endl;

	using stringstream = std::stringstream;
	using ifstream = std::ifstream;
	using ostream = std::ostream;
}